<%= render 'layouts/navbar' %>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <ul class="breadcrumb">
        <li><%= link_to 'Inicio', root_path %></li>
        <li>Productos</li>
      </ul>
      <div class="row page-top">
        <div class="col-sm-6 col-sm-offset-3">
          <h1>Comienza a decorar tu hogar</h1>
          <p class="text-muted">In our Ladies department we offer wide selection of the best products we have found and carefully selected worldwide. Pellentesque habitant morbi tristique senectus et netuss.</p>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <div class="info-bar">
        <div class="row">
          <% total_productos = @products.count %>
          <% lower_limit = ((@products.current_page * 12) - 11) %>
          <% upper_limit = total_productos > (@products.current_page * 12) ? (@products.current_page * 12) : total_productos %>
          <div class="col-sm-12 col-md-4 products-showing"><%="#{lower_limit} - #{upper_limit} de " %><strong><%= total_productos %> productos</strong></div>
          <!-- <div class="col-sm-12 col-md-8 products-number-sort">
            <div class="row">
              <div class="form-inline">
                <div class="col-md-6 col-sm-6">
                  <div class="products-number"><strong>Show</strong> <a href="#" class="btn btn-default btn-sm btn-primary">12</a> <a href="#" class="btn btn-default btn-sm">24</a> <a href="#" class="btn btn-default btn-sm">All</a> <span class="hidden-md">products</span></div>
                </div>
                <div class="col-md-6 col-sm-6">
                  <div class="products-sort-by"><strong>Sort by</strong>
                    <select name="sort-by" class="form-control"><option>Price</option><option>Name</option><option>Sales first</option></select>
                  </div>
                </div>
              </div>
            </div>
          </div> -->
          <div class="col-sm-12 col-md-4 pages">
            <%= will_paginate @products, renderer: BootstrapPagination::Rails, :previous_label => t("previous"), :next_label => t("next") %>
          </div>
        </div>
      </div>
      <div class="row products">
        <!-- product-->
        <% @products.each do |product| %>
          <div class="col-md-4 col-sm-6">
            <div class="product">
              <div class="image"><a href="detail.html"><%= image_tag("lampara-dorada.jpg", class: "img-responsive") %></a>
                <div class="quick-view-button"><%= link_to 'Vista rápida', product_quick_view_path(id: product.id), remote: true, class:"btn btn-default btn-sm quick-view-btn" %></div>
              </div>
              <div class="card-text">
                <!-- <p class="brand"><a href="#">Quiksilver</a></p> -->
                <h3><%= link_to product.name, product_path(product) %></h3>
                <p class="price"><%="#{product.points} puntos"%></p>
              </div>
            </div>
          </div>
        <% end %>
        <!-- /product-->
      </div>

      <div class="pages">
        <%= will_paginate @products, renderer: BootstrapPagination::Rails, :previous_label => t("previous"), :next_label => t("next") %>
      </div>

      <div class="row">
        <div class="col-md-12 banner"><a href="#"><%= image_tag("banner2.jpg", class: "img-responsive") %></a></div>
      </div>
    </div>
  </div>
</div>


